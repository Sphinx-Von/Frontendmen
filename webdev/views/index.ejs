<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hi i am rubina</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen 
    bg-zinc-900">
    <div class="form p-10 text-white">
        <form action="/create" method="post">
            <input class="w-full rounded-md mt-2 block px-5 py-2 bg-zinc-800 text-white" type="text"
            placeholder="Enter Title Here"  name="title">
            <textarea class="w-full rounded-md mt-2 block px-5 py-2 outline-none bg-zinc-800 resize-none text-white"  placeholder="Write your task" name="details"></textarea>
            
            <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Add Task">
        </form>
    </div>

   <div class="tasks flex gap-3 flex-wrap p-10">
    <% if(files.length > 0) {%>
        <% files.forEach(function(file){ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
        <h1 class="text-white text-3xl tracking-tighter"><%=file %></h1>
        <a  class="text-blue-600 inline-block mt-3" href="/file/<%=file %>">Read More</a>
    </div>
       <% }) %>
        <% }else { %>
        
         <h3 class="text-zinc-600">No Tasks Yet</h3>
       <% } %>
    
   </div> 

</body>
</html>